const Loading = () => {
    return (
        <h2 className="text status-message">data not available ... </h2>
        // {data?.length <= 0 && <h2 className="text text-info">loading... </h2>}
    );
}

export default Loading;